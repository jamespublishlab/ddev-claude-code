#ddev-generated
services:
  web:
    env_file:
      - .env.tokens
    volumes:
      - type: bind
        source: ~/.claude
        target: /home/${USER}/.claude
        consistency: cached
        bind:
          create_host_path: false
      - type: bind
        source: ~/.config/gh
        target: /home/${USER}/.config/gh
        consistency: cached
        bind:
          create_host_path: false
